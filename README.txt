SHREE FASHION COLLECTION — DEPLOYMENT PACKAGE

IMPORTANT:
The main index.html now contains the product images directly, so it can be
deployed by itself without losing the images.

WhatsApp:
919649456847 is already connected.

For Vercel:
1. Upload the whole folder OR use index.html as the deployed static file.
2. If using GitHub, keep index.html in the root.
3. No build command is required for this static version.
